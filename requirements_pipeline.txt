# MMRF CoMMpass Data Access & Neoantigen Prediction Pipeline
# Core dependencies

# HTTP & API
requests>=2.31.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0
biopython>=1.81

# Bioinformatics
pyvcf3>=1.0.3

# Machine Learning (MHC binding prediction)
scikit-learn>=1.3.0

# Sequence analysis — MHCflurry is optional but recommended for production-grade
# binding predictions. Requires TensorFlow. Install separately if needed:
#   pip install mhcflurry && mhcflurry-downloads fetch
# The pipeline falls back to PSSM if MHCflurry is not installed.
mhcflurry>=2.1.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
